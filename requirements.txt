# Tested with Python 3.12. Torch-free components (physics chain, analytical
# ablation, validation suite) need only the first four; the neural ablation
# additionally needs torch.
numpy>=1.26
pandas>=2.0
scipy>=1.11
scikit-learn>=1.3
torch>=2.0          # only for scripts/run_full_ablation.py and tests/test_torch_parity.py
